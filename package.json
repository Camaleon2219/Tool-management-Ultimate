{
  "name": "fjk-cnc-tool-management",
  "version": "1.0.0",
  "description": "FJK CNC-Werkzeugverwaltung",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "lint": "node -c server.js",
    "build": "mkdir -p dist && cp index.html manifest.json sw.js dist/ && cp -r icons dist/ && (cp FJK_CNC_Windows_App.zip dist/ 2>/dev/null || true)",
    "start": "node server.js"
  },
  "dependencies": {
    "@google/genai": "^2.22.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "sharp": "^0.35.4"
  }
}
