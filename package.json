{
  "name": "fjk-cnc-tool-management",
  "version": "1.0.0",
  "description": "FJK CNC-Werkzeugverwaltung",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp index.html manifest.json sw.js dist/ && cp -r icons dist/",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "sharp": "^0.35.4"
  }
}
